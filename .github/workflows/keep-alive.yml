name: Keep Render Service Alive

on:
  schedule:
    - cron: "*/10 * * * *"  # every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Curl health endpoint
        run: curl https://launchkit-backend.onrender.com/health